import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'sistem-informasi-11',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
